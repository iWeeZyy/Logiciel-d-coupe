# Transcription locale (CTranslate2, pas de dependance PyTorch).
faster-whisper>=1.2.1

# Numpy < 2 par choix defensif : librosa/numba ont un historique de friction
# avec numpy 2.x via des dependances transitives (voir README, section
# "Notes de compatibilite").
numpy<2

scipy>=1.11
librosa>=0.10.2
soundfile>=0.12

# opencv-python-headless : pas de fenetre GUI necessaire (aucun cv2.imshow ici),
# installation plus legere et evite des soucis de libs GUI/Qt manquantes.
opencv-python-headless>=4.9

# Composition des miniatures (texte accentue mesure et replie). OpenCV ne sait
# rendre que les polices Hershey, sans accents ; ffmpeg drawtext ne sait pas
# mesurer ni replier le texte.
Pillow>=10.0

tqdm>=4.66

# Interface graphique (gui_main.py). QtMultimedia (inclus) sert a la
# previsualisation video integree -- aucune dependance video supplementaire.
PySide6>=6.7

# Recherche YouTube (optionnelle -- necessite internet, voir README).
requests>=2.31
yt-dlp>=2024.8.6

# zoneinfo (stdlib) a besoin de cette base de donnees sur Windows, qui ne
# l'embarque pas nativement -- necessaire pour le reset de quota YouTube
# (minuit heure du Pacifique).
tzdata>=2024.1

# Tests
pytest>=8.0
