# Transcription locale (CTranslate2, pas de dependance PyTorch).
faster-whisper>=1.2.1

# Numpy < 2 par choix defensif : librosa/numba ont un historique de friction
# avec numpy 2.x via des dependances transitives (voir README, section
# "Notes de compatibilite").
numpy<2

scipy>=1.11
librosa>=0.10.2
soundfile>=0.12

# opencv-python-headless : pas de fenetre GUI necessaire (aucun cv2.imshow ici),
# installation plus legere et evite des soucis de libs GUI/Qt manquantes.
opencv-python-headless>=4.9

# Composition des miniatures (texte accentue mesure et replie). OpenCV ne sait
# rendre que les polices Hershey, sans accents ; ffmpeg drawtext ne sait pas
# mesurer ni replier le texte.
Pillow>=10.0

tqdm>=4.66

# Interface graphique (gui_main.py). QtMultimedia (inclus) sert a la
# previsualisation video integree -- aucune dependance video supplementaire.
PySide6>=6.7

# Recherche YouTube (optionnelle -- necessite internet, voir README).
requests>=2.31
yt-dlp>=2024.8.6

# zoneinfo (stdlib) a besoin de cette base de donnees sur Windows, qui ne
# l'embarque pas nativement -- necessaire pour le reset de quota YouTube
# (minuit heure du Pacifique).
tzdata>=2024.1

# Tests
pytest>=8.0

# Synthese vocale LOCALE de Voice Studio : pilote les voix DEJA installees sur
# le systeme (SAPI5 sous Windows, espeak-ng sous Linux, NSSpeechSynthesizer
# sous macOS). Rien n'est telecharge, rien n'est envoye : le texte ne quitte
# pas la machine. Sous Windows, pyttsx3 tire lui-meme comtypes et pywin32.
pyttsx3>=2.90

# Reecriture originale de Voice Studio : execution LOCALE d'un modele GGUF par
# llama.cpp. La bibliotheque seule ne pese que quelques dizaines de megaoctets ;
# les modeles, eux, ne sont jamais embarques -- ils se telechargent depuis
# l'application, sur demande explicite. Licence MIT.
llama-cpp-python>=0.3.0

# Coffre de mots de passe du systeme (Gestionnaire d'identifiants sous Windows).
# Sert UNIQUEMENT a ranger les jetons de connexion aux reseaux sociaux. Sans
# lui, l'application refuse d'enregistrer un jeton -- elle ne l'ecrit jamais en
# clair ailleurs -- et l'export manuel reste disponible.
keyring>=25.0
